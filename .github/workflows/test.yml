name: Email Action Test

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: 24f2002734@ds.study.iitm.ac.in
        run: echo "Hello, world from GitHub Actions!"
